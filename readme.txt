#set up


## set config file
Update settings.cfg with appropriate paths and white list settings. White should be bitbuckets ip list

export WEBHOOK_SETTINGS=/home/logan/projects/webhooks-flask/settings.cfg